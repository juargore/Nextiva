package com.nextiva.nextivaapp.android.listeners;

public interface OnBackPressedListener {

    /**
     * @return True if the listener has consumed the event, false otherwise.
     */
    boolean doOnBackPressed();
}
